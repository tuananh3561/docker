version: '3.9'

services:
  mongodb:
    image: mongo:5.0
    ports:
      - 27017:27017
    volumes:
      - /Users/tuananh/Projects/docker/mongobd_docker/mongodb/database:/data/db
    environment:
      - MONGO_INITDB_ROOT_USERNAME=tuananh
      - MONGO_INITDB_ROOT_PASSWORD=300695
